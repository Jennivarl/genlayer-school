# Contracts

This package holds GenLayer Intelligent Contract templates for GenLayer School.

The first contract is a simple certificate issuer shape. It is intentionally small so we can evolve it after the learning and certification flows are clear.
