NEXT_PUBLIC_APP_URL=http://localhost:3100

# Privy auth
NEXT_PUBLIC_PRIVY_APP_ID=
NEXT_PUBLIC_PRIVY_CLIENT_ID=
PRIVY_APP_ID=
PRIVY_APP_SECRET=
# Optional fallback/override for server JWT verification.
PRIVY_VERIFICATION_KEY=
PRIVY_AUTH_REQUIRED=false

# Required to protect /admin and /api/admin/* in production.
ADMIN_ACCESS_TOKEN=

# Backend storage driver: local | supabase | auto
# Production should use supabase. Local development can use auto or local.
GENLAYER_SCHOOL_STORAGE_DRIVER=auto

# Required only when GENLAYER_SCHOOL_STORAGE_DRIVER=supabase or when using auto with Supabase enabled.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
